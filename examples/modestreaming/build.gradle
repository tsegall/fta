plugins {
	id 'application'
}

wrapper {
	gradleVersion = '8.12'
}

repositories {
	mavenCentral()
}

dependencies {
	implementation group: 'com.cobber.fta', name: 'fta', version: '15.+'
}

application {
	mainClass = 'modestreaming.ModeStreaming'
}
