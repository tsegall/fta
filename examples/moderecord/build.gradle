plugins {
    id 'application'
}

wrapper {
	gradleVersion = '8.0.2'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation group: 'com.cobber.fta', name: 'fta', version: "13.+"
}

application {
    mainClass = 'moderecord.ModeRecord'
}
