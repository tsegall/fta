plugins {
	id 'application'
}

wrapper {
	gradleVersion = '9.0.0'
}

repositories {
	mavenCentral()
}

dependencies {
	implementation group: 'com.cobber.fta', name: 'fta', version: '17.+'
}

application {
	mainClass = 'performance.Performance'
}
